# This file is no longer used - HTTPS configuration is now in:
# - redirect.conf (HTTP to HTTPS redirect)
# - https.conf (HTTPS server with SSL)
# 
# Keeping this file to prevent Elastic Beanstalk errors,
# but all configuration has been moved to the files above.
